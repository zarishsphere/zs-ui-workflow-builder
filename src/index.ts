// ZarishSphere ui-workflow-builder microfrontend entry
import { version } from './version';

console.log('zs-ui-workflow-builder microfrontend starting, version:', version);

// Microfrontend bootstrap placeholder
export default version;
